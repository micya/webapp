apiVersion: v1
kind: Service
metadata:
  name: {{ include "webapp-linux.fullname" . }}
  labels:
    {{- include "webapp-linux.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webapp-linux.selectorLabels" . | nindent 4 }}
