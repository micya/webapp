apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp-windows
spec:
  selector:
    matchLabels:
      app: webapp
  template:
    metadata:
      labels:
        app: webapp
    spec:
      nodeSelector:
        "beta.kubernetes.io/os": "windows"
      containers:
      - name: webapp-windows
        image: micya/webapp:windows
        # resources:
        #   limits:
        #     memory: "128Mi"
        #     cpu: "500m"
        ports:
        - containerPort: 80
        imagePullPolicy: Always
