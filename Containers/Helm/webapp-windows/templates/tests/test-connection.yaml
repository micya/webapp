apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "webapp-windows.fullname" . }}-test-connection"
  labels:
    {{- include "webapp-windows.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "webapp-windows.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
