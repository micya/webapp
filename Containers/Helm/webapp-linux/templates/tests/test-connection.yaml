apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "webapp-linux.fullname" . }}-test-connection"
  labels:
    {{- include "webapp-linux.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "webapp-linux.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
